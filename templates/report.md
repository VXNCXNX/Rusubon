# <one quantified line>

priority: P2
priority_explanation: <one sentence that cites a number>
actionability: requires_human_input

## Hook

What's happening, with numbers.

## Pattern

Why this is concentration on one path, not site-wide wobble.

## Hypothesis

Suspected cause. Not a license to open a PR.

## Evidence

- path:
- element: (slugified; quote raw `$el_text` short if needed)
- step vs baseline:
- persons: (≥5)
- recordings: (2–3 ids)

## Series

Markdown table of numbers you already queried. Recordings vs traffic for a cliff. Daily rage or error-after-click rate for a cluster. Pin calendar dates, not `now()-7d`. The hook above must stand if this table is ignored.

| day | numerator | denominator | rate |
| --- | --- | --- | --- |
| YYYY-MM-DD |  |  |  |

## Query

The HogQL you already ran for that table. A human must be able to re-run it. Pin calendar dates. Do not invent event names. If you used two queries (capture join + rage), paste both.

```sql

```

## Recommendation

What a human should look at next.
